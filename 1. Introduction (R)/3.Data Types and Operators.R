
#numeric implicit declaration
delta = 8
class(delta)

#L declared as int

delta = 8L
class(delta)

#Another types

logico = TRUE #all caps or just the initial
logico = F
caractere = "Text" #single or double quotes
caractere = 'Text'
class(caractere)
class(logico)

#math operations

c = 3
d = 5
(c + d) ** d
e = (c + d) * d
print(e)

#Logical tests
a = 1
b = 2
a < b
a == b


#square root
sqrt(2500)# doesn't need a package, already exist a package for math operations

